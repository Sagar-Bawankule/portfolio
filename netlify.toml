[build]
command = "npm run build"
publish = "out"

# Optional: Set Node.js version
[build.environment]
NODE_VERSION = "18"

# Optional: Handle SPA routing
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
